let ele=document.querySelectorAll("div button")

ele.forEach((e,index)=>{
    if(index === 0)
    {
        e.onclick=function(){
            window.location="music.html"
        }
    }
    if(index === 1)
    {
        e.onclick=function(){
            window.location="music.html"
        }
    }
    if(index === 2)
    {
        e.onclick=function(){
            window.location="music.html"
        }
    }
    if(index === 3)
    {
        e.onclick=function(){
            window.location="music.html"
        }
    }
})

    // element.forEach((ele, index) => {
    //     if (index  === 0) {
    //       ele.value = window.localStorage.getItem("email-value");
    //     }

